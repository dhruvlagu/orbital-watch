# ORBITAL WATCH (Vite + React + TypeScript)

Foundational design system + layout shell for **[SITE NAME — placeholder for now]**.

## Stack
- Vite
- React + TypeScript
- React Router (routes mirror navbar structure)
- Global CSS design tokens and utility classes

## What is implemented
- Global color and typography variables
- Reusable styles: `card`, `btn`, `badge`
- Fixed navbar with active link styling and mobile fullscreen menu
- Footer with three-column structure and source attribution row
- Home placeholder: "Hero section coming next"
- Placeholder route pages for all top-level nav links

## Run locally
From this folder:

```bash
npm install
npm run dev
```

Then open the Vite URL shown in terminal (typically `http://localhost:5174`).

